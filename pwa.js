let prompt;const status=document.getElementById('pwa-status'),button=document.getElementById('pwa-install');window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();prompt=e;button.hidden=false;});button.onclick=async()=>{if(prompt){await prompt.prompt();prompt=null;button.hidden=true;}};if('serviceWorker' in navigator){navigator.serviceWorker.register('./sw.js').then(async registration=>{await navigator.serviceWorker.ready;status.textContent='พร้อมใช้ออฟไลน์ · ไฟล์โครงการเก็บในเบราว์เซอร์นี้';if(registration.waiting)status.textContent+=' · มีอัปเดต ปิดทุกหน้าต่างแอปแล้วเปิดใหม่';}).catch(()=>status.textContent='เตรียมออฟไลน์ไม่สำเร็จ ตรวจการเชื่อมต่อแล้วเปิดใหม่');}else status.textContent='ต้องเปิดผ่าน HTTPS หรือ localhost';